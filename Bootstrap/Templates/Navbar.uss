.navbar {
  color: var(--primary-color);
  padding-left: 16px;
  padding-right: 16px;
}

.nav .border {
  position: absolute;
  border-top-width: var(--border-width);
  width: 100%;
  height: 0px;
  border-top-color: var(--border-color);
  bottom: 0;
  margin: 0;
}